#include <stdio.h>

int main() {
    int N, result = 0, num;
    scanf("%d", &N);
    
    for (int i = 0; i < N; i++) {
        scanf("%d", &num);
        result ^= num;
    }
    
    printf("%d\n", result);
    return 0;
}
